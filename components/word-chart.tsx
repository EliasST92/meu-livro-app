'use client';
import { ResponsiveContainer, AreaChart, Area, XAxis, YAxis, Tooltip } from 'recharts';
export default function WordChart({total}:{total:number}){const base=Math.max(total,100);const data=[{d:'Seg',p:Math.round(base*.12)},{d:'Ter',p:Math.round(base*.2)},{d:'Qua',p:Math.round(base*.35)},{d:'Qui',p:Math.round(base*.48)},{d:'Sex',p:Math.round(base*.66)},{d:'Sáb',p:Math.round(base*.82)},{d:'Hoje',p:base}];return <div className="h-44 w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{top:16,right:8,left:-24,bottom:0}}><defs><linearGradient id="violet" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#7c3aed" stopOpacity={.35}/><stop offset="95%" stopColor="#7c3aed" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="d" tickLine={false} axisLine={false} tick={{fontSize:10}} interval="preserveStartEnd"/><YAxis tickLine={false} axisLine={false} tick={{fontSize:10}}/><Tooltip contentStyle={{fontSize:11,borderRadius:10}}/><Area type="monotone" dataKey="p" stroke="#7c3aed" strokeWidth={3} fill="url(#violet)" isAnimationActive/></AreaChart></ResponsiveContainer></div>}
