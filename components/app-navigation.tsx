'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { signOut, useSession } from 'next-auth/react';
import { BookOpen, Library, Sparkles, Crown, LogOut, Menu, X, Users2, UserCog } from 'lucide-react';
import { useState } from 'react';

export function AppNavigation(){const path=usePathname();const {data:session}=useSession()||{};const [open,setOpen]=useState(false);const items=[{href:'/dashboard',label:'Meus livros',icon:Library},{href:'/comunidade',label:'Comunidade',icon:Users2},{href:'/planos',label:'Plano e recursos',icon:Crown}];return <><header className="sticky top-0 z-40 flex h-16 items-center justify-between bg-white/90 px-5 shadow-sm backdrop-blur-xl lg:hidden"><Link href="/dashboard" className="flex items-center gap-2 font-display font-bold"><BookOpen className="text-violet-700"/>Meu Livro</Link><button onClick={()=>setOpen(v=>!v)} aria-label="Abrir menu">{open?<X/>:<Menu/>}</button></header><aside className={`${open?'flex':'hidden'} fixed inset-x-3 top-20 z-30 flex-col rounded-2xl bg-[#24212a] p-4 text-white shadow-xl lg:inset-y-4 lg:left-4 lg:top-4 lg:flex lg:w-64`}><Link href="/dashboard" className="flex items-center gap-3 px-3 py-3 font-display text-xl font-bold"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-600"><BookOpen size={20}/></span>Meu Livro</Link><nav className="mt-8 space-y-2">{items.map(({href,label,icon:Icon})=><Link key={href} href={href} onClick={()=>setOpen(false)} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold transition ${path===href?'bg-white text-[#24212a]':'text-stone-300 hover:bg-white/10 hover:text-white'}`}><Icon size={19}/>{label}</Link>)}<Link href="/ideias" onClick={()=>setOpen(false)} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold transition ${path==='/ideias'?'bg-white text-[#24212a]':'text-stone-300 hover:bg-white/10 hover:text-white'}`}><Sparkles size={19}/>Ideias com IA</Link><Link href="/conta" onClick={()=>setOpen(false)} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold transition ${path==='/conta'?'bg-white text-[#24212a]':'text-stone-300 hover:bg-white/10 hover:text-white'}`}><UserCog size={19}/>Minha Conta</Link></nav><div className="mt-auto rounded-xl bg-white/10 p-4"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-violet-500 font-bold">{session?.user?.name?.[0]?.toUpperCase?.()??'A'}</span><div className="min-w-0"><p className="truncate text-sm font-semibold">{session?.user?.name??'Autor'}</p><p className="text-xs text-stone-400">{session?.user?.isPremium?'Premium':'Plano gratuito'}</p></div></div><button onClick={()=>signOut({callbackUrl:'/login'})} className="mt-4 flex w-full items-center gap-2 rounded-lg px-2 py-2 text-xs text-stone-300 hover:bg-white/10"><LogOut size={15}/>Sair</button></div></aside></>}
