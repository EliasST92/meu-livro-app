import type { MetadataRoute } from 'next';import { headers } from 'next/headers';export const dynamic='force-dynamic';export default function sitemap():MetadataRoute.Sitemap{const h=headers();const host=h.get('x-forwarded-host')??h.get('host')??'';const protocol=h.get('x-forwarded-proto')??'https';const base=host?`${protocol}://${host}`:(process.env.NEXTAUTH_URL??'http://localhost:3000');return [{url:base,changeFrequency:'monthly',priority:1},{url:`${base}/login`,changeFrequency:'yearly',priority:.4},{url:`${base}/registro`,changeFrequency:'yearly',priority:.5},{url:`${base}/vendas`,changeFrequency:'weekly',priority:.9},{url:`${base}/vitrine`,changeFrequency:'daily',priority:.8}]}
